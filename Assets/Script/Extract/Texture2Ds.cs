using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;
namespace Extract1
{
    public static class Texture2Ds
    {

        //public static Texture2D FromArray(this Texture2D tex, byte[] data, IntVec2 size)
        //{
        //    Texture2D texture = new Texture2D(size.Width, size.Height);
        //    var ms = new MemoryStream(data);
        //    int length = size.Width * size.Height;
        //    Color32[] color32s = new Color32[length];
        //    for (var i = length - 1; i >= 0; i--)
        //    {
        //        Colors.ReadColor(ms, color32s, i);
        //    }
        //    ms.Close();
        //    texture.SetPixels32(color32s);
        //    texture.Apply();
        //    return texture;
        //}

    }
}